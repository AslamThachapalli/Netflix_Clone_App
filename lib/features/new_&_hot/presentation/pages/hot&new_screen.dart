import 'package:flutter/material.dart';

class HotNewScreen extends StatelessWidget {
  const HotNewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('HotNewScreen'),
    );
  }
}
